# benchmarks

Benchmark stubs.

Out of scope for validation work: benchmarks are currently stubs
only, with no harness guarantees. Keep them compiling; do not
route compiler error paths through them.
